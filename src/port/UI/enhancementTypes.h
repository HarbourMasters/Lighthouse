#pragma once

typedef enum {
    BOOTSEQUENCE_DEFAULT,
    BOOTSEQUENCE_AUTHENTIC,
    BOOTSEQUENCE_FILESELECT,
} BootSequenceType;

typedef enum {
    LANGUAGE_ENG,
    LANGUAGE_FRE,
    LANGUAGE_GER,
} LanguageType;
